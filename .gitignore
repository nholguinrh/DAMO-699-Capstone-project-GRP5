proposal_rubric_analysis.md

# Python
__pycache__/
*.pyc
*.pyo

# Environment secrets (each teammate creates their own — see .env.example)
.env


# Raw API cache (regenerated by the pipeline — see data/README.md)
data/raw/*.json
